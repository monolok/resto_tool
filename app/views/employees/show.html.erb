<%= @employee.name%>
<%= @employee.position%>
</br>
<% @employee.scores.each do |r|%>
	<%= r.created_at.strftime("%A %B %d")%>
	<%if r.average > 2.5 %>
		<p class="bg-success"><%= r.average%></p>
	<%else%>
		<p class="bg-danger"><%= r.average%></p>
	<%end%>
<%end%>

<%= link_to 'Edit', edit_employee_path(@employee) %> |
<%= link_to 'Back', employees_path %>
